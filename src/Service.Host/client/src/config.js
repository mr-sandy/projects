const config = {
    //appRoot: PROCESS.ENV.appRoot ? PROCESS.ENV.appRoot : '',
    //proxyRoot: 'http://app.linn.co.uk',
    openIdAuthority: 'https://www-sys.linn.co.uk/auth'
}

export default config;